import React from 'react';
import Form from '../component/Form.jsx'; // Corrected the import statement

const AddEmployee = () => {
    return (
        <div>
            <Form />
        </div>
    );
};

export default AddEmployee;
